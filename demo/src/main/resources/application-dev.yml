spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=base_datos;encrypt=true;trustServerCertificate=true;
    username: sa
    password: Tu_Password_Fuerte_123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.SQLServerDialect
      database: sql_server
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/safco

server:
  port: 8088

